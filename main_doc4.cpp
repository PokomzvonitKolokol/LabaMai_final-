// Считает и выводит среднюю длину текста заметок отдельно для каждого приоритета:
// низкий (1), средний (2), высокий (3).
// Если заметок с каким-то приоритетом нет - этот приоритет не выводится.
//void averageLlengthbypriority(Note notes[], int n);

// Выводит только те заметки, у которых:
//  приоритет не ниже minPriority,
//  длина текста от minLen до maxLen (включительно).
// Если ничего не подходит — пишет "(ничего не найдено)".
//void filterbypriorityandlength(Note notes[], int n, Priority minPriority, int minLen, int maxLen);

// Проверяет, есть ли подстрока substr внутри строки str.
// Учитывает регистр (например, "Привет" ≠ "привет").
// Если substr пустая - всегда возвращает true.
//bool contains(const char* str, const char* substr);

// Ищет в заметках текст, содержащий подстроку substr .
// Выводит  текст таких заметок.
// Если ничего не найдено - пишет "(ничего не найдено)".
//void searchsubstring(Note notes[], int n, char substr[]);

// Сортирует заметки так:
// 1) сначала по приоритету (от высокого к низкому)
// 2) если приоритеты одинаковые - по длине текста от короткого к длинному.
//void sortnotes(Note notes[], int n);